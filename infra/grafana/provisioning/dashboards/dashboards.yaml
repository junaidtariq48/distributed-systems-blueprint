apiVersion: 1

providers:
  - name: "ds-blueprint"
    orgId: 1
    folder: "DS Blueprint"
    type: file
    disableDeletion: true
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
